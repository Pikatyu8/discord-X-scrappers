# discord & X scrappers
for this to work you need to insall `pip install -r requirements.txt"` and then `scrapling install`

run `pre-main.py` first, login to your X or discord account, then run **other files**
