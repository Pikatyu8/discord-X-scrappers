# discord & X scrappers
for this to work you need to insall `pip install playwright requests scrapling yt-dlp` and `python -m pip install "scrapling[all]"` and then `scrapling install`

run `pre-main.py` first, login to your X or discord account, then run **other files**
