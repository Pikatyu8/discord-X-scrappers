# save-my-bookmarks-on-X
for this to work you need to insall `pip install playwright requests scrapling yt-dlp` and `scrapling install`
run `pre-main.py` first, login to your X account, then run `main.py`
